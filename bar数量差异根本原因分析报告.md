# Bar数量差异根本原因分析报告

## 执行摘要

**问题**: on_batch和on_tick处理结果中，616个合约（8%）的bar数量不一致

**根本原因**: **过滤规则不一致** - on_batch和on_tick对volume>0且turnover=0的数据处理不同

**影响范围**: 主要影响早期数据（2010-2011年）的郑商所（ZCE）品种

---

## 1. 问题现象

### 1.1 整体统计
- 总合约数: 8,703
- 数量一致: 8,087 (92%)
- 数量不一致: 616 (8%)

### 1.2 差异最大的合约

| 合约 | on_batch | on_tick | 差异 | 比例 |
|------|---------|---------|------|------|
| SR1109 | 84,056 | 2,174 | 81,882 | 97.4% |
| SR1201 | 83,624 | 3,496 | 80,128 | 95.8% |
| WS1201 | 72,692 | 461 | 72,231 | 99.4% |
| CF1205 | 54,963 | 5,472 | 49,491 | 90.0% |

### 1.3 受影响品种
```
郑商所（ZCE）品种严重：
  TA (PTA)    - 72个合约
  SR (白糖)   - 39个合约  
  CF (棉花)   - 36个合约
  FG (玻璃)   - 34个合约
```

---

## 2. 根本原因：过滤规则不一致

### 2.1 on_batch的规则（宽松）
```rust
// src/parquet_processor_on_batch.rs:102
c > 0.0 && t >= 0.0 && b >= 0.0 && a >= 0.0
```
- 要求: turnover >= 0
- ✅ **允许**: volume>0 且 turnover=0

### 2.2 on_tick的规则（严格）
```rust
// src/bar1min_aggregator.rs:547-550  
if !(tick.volume == 0 && (tick.turnover == 0.0 || tick.turnover.is_nan())
    || (tick.volume > 0 && tick.turnover > 0.0)) {
    return false;
}
```
- 要求: volume>0 时，turnover 必须 >0
- ❌ **拒绝**: volume>0 且 turnover=0

### 2.3 早期数据特征

SR1109在2010-03-15的数据：
```
volume范围: 0 ~ 5098  (有成交量)
turnover范围: 0.0 ~ 0.0  ⚠️ 但成交额全部为0！
```

**结果**: 
- on_batch保留 → 84,056 bars
- on_tick过滤 → 只保留2011-05-19之后的数据 → 2,174 bars

---

## 3. 时间范围对比

| 合约 | on_batch时间范围 | on_tick时间范围 | 缺失时段 |
|------|-----------------|----------------|---------|
| SR1109 | 2010-03-15 ~ 2011-09-15 | 2011-05-19 ~ 2011-09-15 | 丢失14个月 |
| SR1201 | 2010-07-15 ~ 2012-01-17 | 2011-05-19 ~ 2012-01-17 | 丢失10个月 |
| WS1201 | 2010-07-23 ~ 2012-01-16 | 2011-01-04 ~ 2012-01-16 | 丢失5个月 |

---

## 4. 建议解决方案

### 方案A: 放宽on_tick规则（推荐）

修改 `bar1min_aggregator.rs:547-550`:
```rust
// 修改后：只拒绝负数turnover，允许0
if tick.turnover < 0.0 {
    return false;
}
```

**优点**: 
- ✅ 保留历史数据
- ✅ 与on_batch一致
  
**缺点**:
- ⚠️ 保留可能有质量问题的数据

---

## 5. 结论

1. 根本原因：**过滤规则不一致**
2. 影响范围：主要是早期郑商所品种
3. 建议：放宽on_tick规则，保持数据完整性

---

**报告生成**: 2025-11-01  
**数据来源**: /data/future_data/dongzheng_data/
