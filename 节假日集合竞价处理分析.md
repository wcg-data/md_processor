# 节假日前后集合竞价处理分析报告

## 一、实际数据验证（rb2412 - 2024年五一劳动节）

### 场景1：节前最后交易日（2024-04-30）
```
数据特征:
  ✓ 无夜盘tick（21:00-05:00时段tick数=0）
  ✓ 仅有日盘交易
  ✓ 有08:59的集合竞价tick（volume=集合竞价成交量）
  
时间线:
  08:59:00  日盘集合竞价  → 生成09:00:00的bar
  09:01:00  连续交易第一个bar
  ...
  15:00:00  收盘bar
  (无夜盘)
```

### 场景2：节后第一个交易日（2024-05-06）  
```
数据特征:
  ✓ 有夜盘集合竞价（20:59:00.500, volume=800）
  ✓ 有日盘集合竞价（08:59:00.500, volume=300）
  ✓ volume在夜盘→日盘之间重置（800→300）
  
时间线:
  20:59:00  夜盘集合竞价  volume=800   → 生成21:00:00的bar
  21:01:00  夜盘连续交易  volume从800累加
  23:00:00  夜盘结束
  ─────────── volume重置 ───────────
  08:59:00  日盘集合竞价  volume=300   → 生成09:00:00的bar (新的累计起点)
  09:01:00  日盘连续交易  volume从300累加
  ...
```

## 二、关键发现

### 发现1：夜盘和日盘的volume是分别累计的
```
夜盘时段: volume从0累计到某个值（如800）
日盘时段: volume重新从0累计（如300）

这是交易所的标准行为：
  - 每个交易时段独立计数
  - 时段切换时volume重置
```

### 发现2：节后第一天会有两个集合竞价
```
IF品种（仅日盘）:
  节后第一天: 1个集合竞价（09:25-09:30）

ag/cu/rb等品种（有夜盘）:
  节后第一天: 2个集合竞价
    - 夜盘集合竞价（20:55-21:00）
    - 日盘集合竞价（08:55-09:00）
```

### 发现3：现有代码已经处理了volume重置
```rust
// bar1min_aggregator.rs:135-139
if last_tick.volume < prev.volume {
    last_tick.volume  // 累计值重置，直接使用当前累计值
} else {
    last_tick.volume - prev.volume  // 正常差分
}
```

## 三、方案A在节假日场景的处理

### 节前最后交易日（停夜盘）

**tick数据**:
```
无20:55-21:00的夜盘集合竞价tick
有08:55-09:00的日盘集合竞价tick
```

**方案A处理**:
```
1. 检测夜盘集合竞价时段（20:55-21:00）
   → 未发现tick，跳过夜盘集合竞价bar生成 ✓

2. 检测日盘集合竞价时段（08:55-09:00）
   → 发现08:59的tick，生成09:00:00的集合竞价bar ✓
   
3. 连续交易从集合竞价累计值开始差分 ✓
```

**结果**: ✅ 正确处理，不会因为配置有夜盘而错误生成21:00的bar

---

### 节后第一个交易日（恢复夜盘）

**tick数据**:
```
有20:55-21:00的夜盘集合竞价tick（volume=800）
有08:55-09:00的日盘集合竞价tick（volume=300，已重置）
```

**方案A处理**:
```
1. 检测夜盘集合竞价时段（20:55-21:00）
   → 发现20:59的tick，生成21:00:00的集合竞价bar
   volume=800, oi_diff=0 ✓
   记录夜盘集合竞价累计值: auction_volume=800

2. 夜盘连续交易（21:01-23:00）
   → volume差分: tick_vol - prev_tick_vol
   21:01的bar: volume = (增量)

3. 检测日盘集合竞价时段（08:55-09:00）
   → 发现08:59的tick，volume=300
   
   关键: 检测到volume重置（300 < 夜盘最后的volume）
   → 触发累计值重置逻辑 ✓
   → 生成09:00:00的集合竞价bar
   volume=300, oi_diff=0 ✓
   
4. 日盘连续交易（09:01-15:00）
   → volume从300的累计值开始差分 ✓
```

**结果**: ✅ 正确处理，会生成两个集合竞价bar（21:00和09:00）

---

## 四、方案A的自适应能力总结

### ✅ 已支持的场景

| 场景 | tick数据特征 | 方案A处理 | 结果 |
|------|------------|----------|------|
| 正常交易日（有夜盘） | 夜盘集合竞价tick + 日盘集合竞价tick | 生成2个集合竞价bar | ✅ |
| 正常交易日（无夜盘） | 仅日盘集合竞价tick | 生成1个集合竞价bar | ✅ |
| 节前停夜盘 | 无夜盘tick，有日盘tick | 仅生成日盘集合竞价bar | ✅ |
| 节后恢复夜盘 | volume重置，有2个集合竞价tick | 生成2个集合竞价bar，正确处理重置 | ✅ |
| 临时停夜盘 | 配置有夜盘，实际无tick | 数据驱动，不生成夜盘bar | ✅ |
| 集合竞价无成交 | 有集合竞价时段tick，但volume=0 | 可选：跳过或生成volume=0的bar | 🟡 |

### ✅ 核心自适应机制

**1. 数据驱动检测**
```rust
// 仅当实际存在集合竞价tick时才生成bar
if auction_ticks.is_empty() {
    return None;  // 跳过集合竞价bar生成
}
```

**2. 累计值重置检测**
```rust
// 自动检测volume重置（跨交易时段）
if last_tick.volume < prev.volume {
    // 累计值重置，直接使用当前累计值
    last_tick.volume
}
```

**3. 多集合竞价支持**
```
每个交易时段独立检测集合竞价：
  - 夜盘开盘前: 20:55-21:00 → 21:00:00 bar
  - 日盘开盘前: 08:55-09:00 → 09:00:00 bar
```

### 🟡 需要注意的边界情况

**情况1：集合竞价无成交（volume=0）**
```
08:59:00  集合竞价tick, volume=0, turnover=0
09:00:00  连续交易开始

方案:
  - 选项A: 跳过volume=0的集合竞价bar（推荐）
  - 选项B: 生成volume=0的bar，标记为集合竞价
```

**情况2：异常tick（如07:03的volume=0）**
```
已由validate_tick过滤掉（volume=0 && turnover=0的tick）
```

**情况3：集合竞价时间变化**
```
交易所调整集合竞价时间（如09:25→09:15）:
  - 需要更新trade_session.csv的auction_time配置
  - 代码自动适配新的时间
```

## 五、实现建议

### 推荐配置

```rust
// 集合竞价bar生成条件
fn should_generate_auction_bar(auction_ticks: &[Tick]) -> bool {
    if auction_ticks.is_empty() {
        return false;  // 无tick，跳过
    }
    
    let last_tick = auction_ticks.last();
    if last_tick.volume == 0 {
        return false;  // 无成交，跳过（可选）
    }
    
    true
}
```

### trade_session.csv配置

```csv
# 确保所有品种都配置了auction_time
comd,auction_time,opening_time,closing_time
ag,20:55:00,21:00:00,02:30:00   # 夜盘集合竞价
ag,08:55:00,09:00:00,10:15:00   # 日盘集合竞价（可选新增行）
IF,09:25:00,09:30:00,11:30:00   # 股指集合竞价
```

**注意**: 当前配置文件每个品种的每个时段只有一行，如果需要在同一天生成两个集合竞价bar（夜盘+日盘），需要考虑配置格式的扩展。

### 简化方案（推荐）

基于当前配置格式，使用隐式规则：
```rust
// 有夜盘的品种，自动检测两个集合竞价时段
if has_night_session(comd) {
    // 夜盘集合竞价: 20:55-21:00
    // 日盘集合竞价: 08:55-09:00（默认）
} else {
    // 仅日盘集合竞价: 根据opening_time推断
}
```

## 六、总结

### ✅ 方案A完全支持节假日场景

1. **节前停夜盘**: 自动跳过夜盘集合竞价bar生成
2. **节后恢复夜盘**: 自动生成两个集合竞价bar（夜盘+日盘）
3. **volume重置**: 已有逻辑支持跨时段重置检测
4. **数据驱动**: 仅在实际有tick时生成bar，避免配置错误

### ✅ 无需额外处理

现有的`volume < prev_volume`检测机制已经完美处理了节假日场景，不需要额外的节假日判断逻辑。

### 建议

1. 在trade_session.csv中为所有品种配置auction_time
2. 对于有夜盘的品种，可以通过代码逻辑自动推断日盘集合竞价时间（08:55-09:00）
3. 添加单元测试验证节假日场景
4. 文档中说明夜盘和日盘的volume是分别累计的特性

