找到了！关键差异在第154-158行：

  // 检查volume是否为0，如果为0则跳过生成集合竞价bar
  if volume == 0 {
      self.auction_ticks.clear();
      return None;
  }

  根本原因总结

  m1803 的差异原因：

  原始数据：
  - 20:55:05: volume=0, oi=0 （集合竞价快照）
  - 21:00:00: volume=0, oi=0 （夜盘开始快照）
  - 21:00:07: volume=4, oi=4 （第一笔交易）

  on_tick 处理：
  1. validate_tick 允许 volume=0 的tick通过 ✅
  2. Bar1MinAggregator 积累集合竞价ticks
  3. flush_auction_bar 检测到 volume=0，跳过生成bar ✅
  4. 21:01的第一个bar：oi=10, oi_diff=0 ✅

  on_batch 处理：
  1. 时间过滤允许 volume=0 的tick通过 ✅
  2. 向量化聚合生成所有bar，包括 volume=0 的bar ❌
  3. fill_missing_minutes 保留这些bar ❌
  4. 21:01的第一个bar：oi=10, oi_diff=10（相对于前一个oi=0的bar）❌

  解决方案

  需要修改 fill_missing_minutes，增加重置第一个 volume>0 bar的 oi_diff 为0的逻辑（就像它已经对 prev_close 和
  log_return 做的那样）。

  这是最合理的解决方案，因为：
  1. fill_missing_minutes 已经处理"第一个有效bar"的逻辑
  2. 修改一处即可同时修复两个处理器
  3. 符合设计原则：第一个真实交易bar的oi_diff应该是0