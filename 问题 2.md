  当前逻辑对比

  flush() - 正常连续交易bar（第213-264行）

  if self.last_tick.is_none() {
      // 当前分钟没有tick，生成空bar
      if let Some(prev_last_tick) = self.prev_last_tick {
          // 使用 prev_last_tick 的值
          BarData {
              open/high/low/close: prev_last_tick.close,
              volume: 0,
              turnover: 0.0,
              open_interest: prev_last_tick.open_interest,
              open_interest_diff: 0,
              ...
          }
      }
  }
  即使没有tick，也会生成空bar ✅

  flush_auction_bar() - 集合竞价bar（第154-158行）

  if volume == 0 {
      self.auction_ticks.clear();
      return None;  // 直接跳过！
  }
  volume=0 时不生成bar ❌

  问题

  这导致了行为不一致！让我看看原始tick数据中 volume=0 时的价格：
