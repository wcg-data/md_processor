# Open Interest (持仓量) 详解

## 1. 什么是Open Interest？

**Open Interest（持仓量）**：某个时刻市场上所有**未平仓合约**的总数。

### 关键特性
1. **时点值**：不是累计值，是某一时刻的瞬时快照
2. **交易所提供**：tick数据中的`open_interest`字段直接来自交易所
3. **全市场统计**：反映整个市场对该合约的持仓情况

### 举例说明
```
时刻1:
  - 张三买入开仓1手，李四卖出开仓1手
  → open_interest = 1 (新增1手持仓)

时刻2:
  - 王五买入开仓2手，赵六卖出开仓2手
  → open_interest = 3 (原有1手 + 新增2手)

时刻3:
  - 张三平仓1手
  → open_interest = 2 (减少1手)
```

## 2. Tick数据中的Open Interest

### 实际数据（rb2412，2024-05-06）

```
夜盘集合竞价：
20:59:00 - volume=800,  oi=56459
21:00:00 - volume=800,  oi=56459 (集合竞价期间oi不变)
21:00:04 - volume=801,  oi=56458 (有人平仓，oi减少1)

日盘集合竞价（次日）：
08:59:00 - volume=865,  oi=56447 (夜盘结束后市场持仓变化)
09:00:00 - volume=865,  oi=56447 (集合竞价期间oi不变)
09:00:03 - volume=866,  oi=56447 (有新成交，但可能是换手)
```

### 关键观察
1. **OI在tick间跳动**：56459→56458→...→56447
2. **OI不一定增加**：可能增加、减少或不变
3. **Volume重置，OI不重置**：夜盘→日盘，volume从800→865重新累计，但OI是连续的

## 3. Bar中的oi_diff计算

### 核心逻辑
```
oi_diff = 当前bar的oi - 上一个bar的oi
```

**目的**：反映这1分钟内市场持仓的**净变化**

### 示例1：连续的bar

```
Tick数据：
21:00:xx - volume累计=800-850, oi从56459变到56458

生成的bar：
21:00:00 bar (集合竞价):
  open_interest = 56459 (集合竞价最后一个tick的oi)
  oi_diff = 0 (第一个bar，无前置bar，无法计算diff)

21:01:00 bar (连续交易):
  open_interest = 56458 (这1分钟最后一个tick的oi)
  oi_diff = 56458 - 56459 = -1 (持仓减少1手)
```

### 示例2：第一个bar的问题（修复前的bug）

```
❌ 修复前（错误）:
第一个bar:
  oi_diff = open_interest本身 (比如56459)

问题：第一个bar没有前置bar，oi_diff应该是0或NULL，
     而不是把oi值本身当作diff

✅ 修复后（正确）:
第一个bar:
  oi_diff = 0

原因：第一个bar是我们观察的起点，无法知道
     在这之前市场持仓变化了多少
```

## 4. 夜盘→日盘切换的情况

### 场景：rb2412, 2024-05-06夜盘 → 2024-05-07日盘

```
夜盘最后的bar（假设22:59）:
  open_interest = 56450
  oi_diff = 56450 - 56458 = -8

--- 夜盘结束，休市期间可能有结算调整 ---

日盘第一个bar（09:00集合竞价）:
  open_interest = 56447 (日盘开盘时的oi)
  oi_diff = ?
```

### 问题：跨时段的oi_diff怎么计算？

**答案**：取决于代码逻辑

#### 方案A：保持连续（当前实现）
```rust
// prev_last_tick保存夜盘最后一个tick
self.prev_last_tick = Some(last_tick);  // oi=56450

// 日盘第一个bar计算
oi_diff = 56447 - 56450 = -3
```

**含义**：反映了夜盘收盘到日盘开盘之间的持仓变化（包括休市期间）

#### 方案B：重置为0（集合竞价bar的实现）
```rust
// 集合竞价bar
oi_diff = 0

// 不更新prev_last_tick，或者在新交易时段开始时重置
```

**含义**：每个交易时段独立计算

### 当前代码的实际行为

```rust
// bar1min_aggregator.rs

// 集合竞价bar（line 156）
open_interest_diff: 0,  // 集合竞价bar的oi_diff=0

// 更新prev_last_tick（line 170）
self.prev_last_tick = Some(last_tick);  // 保存集合竞价最后的tick

// 连续交易bar（line 261）
oi_diff = last_tick.oi - prev.oi  // 基于prev_last_tick计算
```

**结论**：
- 集合竞价bar：`oi_diff=0`（正确，因为是新时段的起点）
- 集合竞价后的第一个连续交易bar：`oi_diff = 当前oi - 集合竞价oi`（正确）

## 5. 完整的示例演示

### 场景：IF1706第一天（2016-10-24）

```
Tick原始数据：
09:29:00 - volume=2, oi=2   (集合竞价)
09:30:05 - volume=2, oi=2   (连续交易开始，无新成交)
09:31:30 - volume=3, oi=3   (有1手新开仓)
09:32:30 - volume=4, oi=4   (有1手新开仓)

生成的1min bar（on_tick）：
┌──────────┬────────┬────┬─────────┬──────────────┐
│ 时间      │ volume │ oi │ oi_diff │ 说明          │
├──────────┼────────┼────┼─────────┼──────────────┤
│ 09:30:00 │ 2      │ 2  │ 0       │ 集合竞价bar   │
│          │        │    │         │ 第一个bar     │
├──────────┼────────┼────┼─────────┼──────────────┤
│ 09:31:00 │ 0      │ 2  │ 0       │ 无成交tick    │
│          │        │    │         │ oi不变        │
├──────────┼────────┼────┼─────────┼──────────────┤
│ 09:32:00 │ 1      │ 3  │ 1       │ 新增1手持仓   │
│          │        │    │         │ 3-2=1        │
├──────────┼────────┼────┼─────────┼──────────────┤
│ 09:33:00 │ 1      │ 4  │ 1       │ 新增1手持仓   │
│          │        │    │         │ 4-3=1        │
└──────────┴────────┴────┴─────────┴──────────────┘

5min聚合（09:35:00）：
  volume = 2+0+1+1+... = 7
  oi = 7 (最后一个1min bar的oi)
  oi_diff = 0+0+1+1+3 = 5

  解释：09:30-09:34这5分钟内，市场净增加了5手持仓
       从oi=2增加到oi=7
```

## 6. 修复总结

### Bug：第一个bar的oi_diff错误

**修复前**：
```rust
// 第一个bar
oi_diff = last_tick.open_interest as i32  // 比如2
```

**问题**：
- 第一个bar没有前置bar，无法计算"变化量"
- 把oi的绝对值当作diff，导致数据不一致
- 如果oi=56459，oi_diff也会是56459（完全错误）

**修复后**：
```rust
// 第一个bar
oi_diff = 0
```

**理由**：
- 第一个bar是观察起点，oi_diff应该为0
- 表示"从我们开始观察起，还没有变化"
- 符合金融数据的惯例（第一个数据点的diff为0或NULL）

## 7. 验证

```python
# IF1706实际数据验证
第一个bar（09:31:00，on_batch）:
  oi=2, oi_diff=0 ✅ (修复后正确)

第二个bar（09:32:00）:
  oi=3, oi_diff=1 ✅ (3-2=1，正确)

5min bar（09:35:00）:
  oi=7, oi_diff=5 ✅ (sum(0+1+1+3+0)=5，正确)
```

## 8. 关键要点总结

1. **OI是时点值**：不是累计值，是市场瞬时持仓量
2. **OI_diff是差分**：当前oi - 上一个bar的oi
3. **第一个bar的oi_diff=0**：因为没有前置bar可以计算差分
4. **夜盘→日盘**：oi连续，但volume重置
5. **集合竞价bar的oi_diff=0**：因为是新时段的起点
6. **5min的oi_diff=sum(1min的oi_diff)**：累加各分钟的变化

希望这样解释清楚了！
