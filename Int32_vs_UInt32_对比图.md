# Int32 vs UInt32 可视化对比

## 📊 数值范围对比

```
UInt32 (无符号32位整数)
┌────────────────────────────────────────────────────┐
│  最小值: 0                                         │
│  最大值: 4,294,967,295 (约42.9亿)                  │
│                                                    │
│  ┌──────────────────────────────────────────────┐ │
│  │ 0  1  2  3  4  ...  42亿                     │ │
│  └──────────────────────────────────────────────┘ │
│                                                    │
│  ❌ 无法表示负数！                                  │
└────────────────────────────────────────────────────┘

Int32 (有符号32位整数)
┌────────────────────────────────────────────────────┐
│  最小值: -2,147,483,648 (约-21.5亿)                │
│  最大值: +2,147,483,647 (约+21.5亿)                │
│                                                    │
│  ┌──────────────────────────────────────────────┐ │
│  │ -21亿 ... -2  -1  0  1  2 ...  +21亿        │ │
│  └──────────────────────────────────────────────┘ │
│                                                    │
│  ✅ 可以表示负数！                                  │
└────────────────────────────────────────────────────┘
```

---

## 🎯 open_interest_diff 为什么必须用 Int32？

### 场景模拟：IF1502 合约的持仓变化

```
时间线:
09:30   09:31   09:32   09:33   09:34   09:35
  │       │       │       │       │       │
  ▼       ▼       ▼       ▼       ▼       ▼
┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐
│ OI  │ │ OI  │ │ OI  │ │ OI  │ │ OI  │ │ OI  │
│25000│ │25500│ │25200│ │25800│ │25100│ │26000│
└─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘
   │       │       │       │       │       │
   │   +500   -300   +600   -700   +900
   │       │       │       │       │       │
   0    正数    负数❗  正数    负数❗  正数
```

**如果使用 UInt32**:
```rust
Bar 1: oi_diff = 0           ✅ 可以表示
Bar 2: oi_diff = +500        ✅ 可以表示
Bar 3: oi_diff = -300        ❌ 溢出！变成 4,294,966,996
Bar 4: oi_diff = +600        ✅ 可以表示
Bar 5: oi_diff = -700        ❌ 溢出！变成 4,294,966,596
Bar 6: oi_diff = +900        ✅ 可以表示
```

**使用 Int32**:
```rust
Bar 1: oi_diff = 0           ✅ 正确
Bar 2: oi_diff = +500        ✅ 正确
Bar 3: oi_diff = -300        ✅ 正确（关键！）
Bar 4: oi_diff = +600        ✅ 正确
Bar 5: oi_diff = -700        ✅ 正确（关键！）
Bar 6: oi_diff = +900        ✅ 正确
```

---

## 📈 实际数据分布

```
IF1502_1min.parquet 的 open_interest_diff 分布
(共 9,922 个 bar)

         负数区域          |         正数区域
   ◄──────────────────────┼──────────────────────►
                          0

   4,230 个 bar (42.6%)   |   5,550 个 bar (55.9%)

   最小: -11,098 手        |   最大: +5,782 手
         ▲                |
         │                |
    如果用 UInt32，       |
    这些全部会溢出！       |
```

---

## 🔢 内存占用对比

```
两者内存占用完全相同：

UInt32: ████ (4字节)
Int32:  ████ (4字节)

区别只在于解释方式：
UInt32: 全部32位用于数值，范围 0 到 42亿
Int32:  最高位是符号位，范围 -21亿 到 +21亿
```

---

## 💡 二进制表示对比

```
数值: -300

Int32 表示 (正确):
  符号位: 1 (负数)
  数值位: 00000000000000000000000100101100
  ───────────────────────────────────────
  结果: -300 ✅

UInt32 误用 (错误):
  全部是数值位
  11111111111111111111111011010100
  ───────────────────────────────────────
  结果: 4,294,966,996 ❌ (溢出成巨大正数)
```

---

## 🎓 字段类型设计决策树

```
┌──────────────────────────────────┐
│  这个字段的值可能是负数吗？      │
└────────────┬─────────────────────┘
             │
      ┌──────┴──────┐
      │             │
     YES           NO
      │             │
      ▼             ▼
┌──────────┐  ┌──────────┐
│ 使用 Int │  │ 使用 UInt│
└──────────┘  └──────────┘
      │             │
      ▼             ▼
  例如:          例如:
  - oi_diff      - volume
  - price_change - open_interest
  - profit_loss  - bid_volume_1
  - temperature  - user_id
```

---

## 📋 完整字段类型速查表

```
┌──────────────────┬─────────┬──────────────────┐
│ 字段              │ 类型    │ 原因             │
├──────────────────┼─────────┼──────────────────┤
│ volume           │ UInt32  │ 成交量 ≥ 0       │
│ open_interest    │ UInt32  │ 持仓量 ≥ 0       │
│ bid_volume_1     │ UInt32  │ 买一量 ≥ 0       │
│ ask_volume_1     │ UInt32  │ 卖一量 ≥ 0       │
├──────────────────┼─────────┼──────────────────┤
│ oi_diff          │ Int32   │ 可正可负 ✅      │
├──────────────────┼─────────┼──────────────────┤
│ maturity_month   │ Int16   │ 可负（已过期）   │
│ maturity_day     │ Int16   │ 可负（已过期）   │
└──────────────────┴─────────┴──────────────────┘
```

---

## ⚠️ 常见错误示例

### 错误1: 差分字段使用无符号类型

```rust
❌ 错误
struct Bar {
    open_interest: u32,
    open_interest_diff: u32,  // ❌ 减仓时会溢出
}

✅ 正确
struct Bar {
    open_interest: u32,
    open_interest_diff: i32,  // ✅ 可以表示负数
}
```

### 错误2: 永远非负的字段使用有符号类型

```rust
❌ 浪费空间
struct Bar {
    volume: i32,  // 成交量永远 ≥ 0，浪费了一半范围
}

✅ 更好
struct Bar {
    volume: u32,  // 范围：0 到 42亿
}
```

---

## 🎯 总结

### 一句话记忆法

```
需要负数 → Int
永远非负 → UInt
差分变化 → Int（因为可能减少）
数量计数 → UInt（因为不能为负）
```

### 实际验证

```bash
# 查看实际数据中的负数占比
python3 -c "
import pyarrow.parquet as pq
df = pq.read_table('IF1502_1min.parquet').to_pandas()
neg_count = (df['open_interest_diff'] < 0).sum()
total = len(df)
print(f'负数占比: {neg_count}/{total} = {neg_count/total*100:.1f}%')
"

# 输出：负数占比: 4230/9922 = 42.6%
# ↑ 证明必须用 Int32！
```
