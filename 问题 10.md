觉得你check  个 on_tick  吧  参考 on_batch 里面的 处理要求, 读取 trade_session 正确处理集合竞价的 bar,  如果一个tick 的 volume 和 
turnover 是 0  那么 ohlc使用上一个有效的 close  oi 使用上一个有效的 oi,  过滤不在交易时间段且不在集合竞价时间段的 tick, fill_missing 
可能是有问题的 你再分析一下 如果非交易时间 非集合竞价的时间不需要 fill, 


--------------------------

现在让我总结一下问题：

  发现的问题

  我的修改导致volume计算完全错误！

  | 版本         | 22:54 | 22:55 | 22:56 | 22:57 | 22:58 | 22:59 | 23:00 | 23:01 |
  |------------|-------|-------|-------|-------|-------|-------|-------|-------|
  | 原始on_tick  | 2732  | 1703  | 1805  | 2122  | 2237  | 3615  | 5523  | 8     |
  | 修改后on_tick | 2621  | 4477  | 4356  | 4257  | 5044  | 6682  | 13705 | -     |
  | on_batch   | 2732  | 1703  | 1805  | 2122  | 2237  | 3615  | 5531  | -     |

  原始版本：
  - ✅ volume计算正确（除了23:01 bar不应该存在）
  - ❌ 生成了23:01 bar（8手）

  我的修改版本：
  - ✅ 不生成23:01 bar
  - ❌ volume计算完全错误（几乎翻倍）

  问题分析

  我需要仔细分析为什么我的修改会破坏volume计算。可能的原因：

  1. prev_last_tick的传递问题？
  2. 集合竞价bar的影响？
  3. current_bar_minute的使用问题？

  您能帮我看看吗？或者您有什么想法为什么会这样？原始版本的volume是正确的，说明差分计算本身没问题，但我的修改引入了某种bug。

> volume 都偏大的原因可能是  比如说夜盘的集合竞价里面的 turnover 和 volume 都是重置的,但是日盘的集合竞价里面的 turnover 和 volume 
是获取到的累计值,所以 on_tick 如果检测到日盘的集合竞价 也需要 对 turnover 和 volume 作差分, 你看看 
是不是你的修改逻辑导致这部分的逻辑没有正确运行 

● 您说得太对了！我明白问题了！

  问题根源：我的修改中，收盘tick累积后返回了None（没有flush），导致：

  if is_closing_tick {
      self.last_tick = Some(*tick);  // 更新为收盘tick
      // 更新high/low...
      return None;  // ❌ 没有flush！
  }

  导致的问题：
  1. last_tick更新为23:00:00的收盘tick
  2. 但没有flush，所以prev_last_tick没有更新
  3. prev_last_tick还是22:58或更早的tick
  4. 下一个交易日集合竞价到来时触发flush
  5. volume差分 = 23:00:00.volume - 22:58.volume = 巨大的值！❌

  正确的修复应该是：

  if is_closing_tick {
      self.last_tick = Some(*tick);
      // 更新high/low...

      // ✅ 立即flush生成收盘bar
      let flushed = self.flush();
      // flush会自动设置：prev_last_tick = self.last_tick (收盘tick)
      // flush会清空：current_bar_minute, last_tick

      // ✅ 不init新窗口（等待下一个tick）
      return flushed;
  }

  这样：
  1. ✅ 收盘tick被包含在23:00 bar中
  2. ✅ prev_last_tick正确更新为收盘tick
  3. ✅ trade_time = current_bar_minute + 1 = 23:00（正确！）
  4. ✅ 不生成23:01 bar（因为没有init新窗口）
  5. ✅ 下一个集合竞价的volume差分计算正确

  我之前的错误是想"等待文件结束再flush"，但这破坏了prev_last_tick的传递！应该立即flush！

  让我立即修复：