  align_to_bar_minute 定义在 src/common_utils.rs:45:

  pub fn align_to_bar_minute(ts: &DateTime<Utc>) -> DateTime<Utc> {
      ts.with_second(0).unwrap().with_nanosecond(0).unwrap() + chrono::Duration::minutes(1)
  }

  这个函数：
  1. 去除秒和纳秒
  2. 加1分钟

  所以15:00 → 15:01！

  但理论上，收盘tick（15:00:00）应该：
  1. 被检测为收盘时间
  2. tick_minute减1，分配到14:59窗口
  3. current_bar_minute设为14:59
  4. flush时：14:59 + 1min = 15:00 ✅

  让我检查current_bar_minute在on_tick中是如何设置的：
现关键差异了！

  - 生成15:00的日期：有3-4个ticks（包括其他非标准时间的ticks如07:10、22:15），会触发fill_missing_minutes完整填充
  - 生成15:01的日期：只有2个标准ticks（15:00+20:59），不触发完整fill

  这证实了：问题的根源是is_closing_time没有识别15:00:00为收盘时间！

  你说得对，is_closing_time逻辑本身应该没问题（大部分品种都正确）。让我检查一下parse_datetime_from_tick函数，看是否解析
  后的时间和trade_session配置的时间有格式差异：


  


 分析结果非常清晰：

  核心问题

  100%是空bar（volume=0）的差异！

  以ZC2501为例：
  - 非零volume bars: on_batch=0, on_tick=0 （完全没有成交！）
  - 差异全部来自空bar的填充不一致
  - on_batch多了 15,470个空bar（主要是13:30-13:59时段）
  - on_tick多了 170个空bar（15:01，不应该存在的收盘后bar）

  问题模式

  Pattern 1: on_batch多填充13:30-13:59
  on_batch多的bars:
    2024-01-09 13:30:00 ~ 13:59:00 (volume=0)
    ...
  这些合约完全没有交易，但on_batch仍填充了完整交易时段的空bar

  Pattern 2: on_tick错误填充15:01
  on_tick多的bars:
    2024-01-09 15:01:00 (volume=0)  ❌ 收盘后bar
    ...

  受影响品种

  严重问题（无成交合约）:
  - ZC（动力煤）: 4个合约，0个非零volume bars，全是空bar
  - wr（线材）: 4个合约，14个非零volume bars，几乎无成交

  这些是已停止交易或极低流动性的合约。

  ● 🎯 根本原因找到了！

  原始tick数据显示：
  - ZC2501只有 423个ticks（停止交易的合约）
  - 每天只有 2个静态tick：
    - 15:00:00（日盘收盘）
    - 20:59:00（夜盘前）
  - 所有 volume=0，close=801.4（无成交）

  问题分析

  问题1: on_tick生成了15:01 bar

  原始tick有15:00:00（收盘tick），on_tick应该：
  - 将15:00:00合并到14:59窗口 → 生成15:00 bar
  - 但实际生成了15:01 bar ❌

  这说明收盘tick检测有问题，或者flush时trade_time计算错误！

  让我检查 is_closing_time 是否正确识别了15:00:


  好消息 ✅：
  1. 收盘tick修复完全成功！
    - 收盘时间点volume: 260/260 (100%) 完全一致
    - 集合竞价volume: 189/189 (100%) 完全一致
    - Bars总数: 238/265 (89.8%) 完全一致
  2. 不一致的27个合约（10.2%）都是停止交易/极低流动性合约：
    - ZC（动力煤）：4个合约，完全无成交
    - wr（线材）：4个合约，只有14个非零volume bars
    - 差异100%来自空bar（volume=0）填充，不影响数据质量

  🔍 发现的新问题

  问题：on_tick会为低流动性合约生成收盘后bar

  以ZC2501为例：
  - 原始tick: 只有1个 15:00:00（收盘tick）
  - on_tick生成: 15:01:00 bar ❌ （不应该存在）

  根本原因：is_closing_time 函数未能正确识别15:00:00为收盘时间，可能是时间精度问题（微秒）。

  📝 已创建的文件

  1. ZC合约15_01_bar问题分析报告.md - 完整分析报告
  2. analyze_mismatch_contracts.py - 不一致合约深入分析脚本
  3. test_zc_15_01_bar.py - ZC 15:01 bar问题测试脚本

  🔧 修复建议

  高优先级（影响数据正确性）：
  修复 is_closing_time 的时间比较，去除微秒影响：
  let tick_time = tick_datetime.time().with_nanosecond(0).unwrap();
  let end_time = end.with_nanosecond(0).unwrap();
  if tick_time == end_time {
      return true;
  }

  低优先级（不影响数据质量）：
  统一空bar填充逻辑（只影响停止交易合约）

  ---
  总体评估：✅ on_tick收盘tick修复对有成交合约完全成功！只需修复低流动性合约的边界情况