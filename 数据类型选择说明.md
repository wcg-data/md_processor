# 数据类型选择说明

**版本**: v1.0
**更新日期**: 2025-11-01

---

## 🎯 核心问题：为什么 open_interest_diff 用 Int32 而不是 UInt32？

### 简短回答
**因为持仓变化可以是负数！**

```
持仓减少时:
  当前持仓 25,200 - 前一持仓 25,500 = -300 ← 负数，UInt32无法表示
```

---

## 📊 Int32 vs UInt32 对比

| 特性 | UInt32 | Int32 |
|------|--------|-------|
| **全称** | Unsigned Integer 32-bit | Signed Integer 32-bit |
| **位数** | 32位 | 32位 |
| **内存占用** | 4字节 | 4字节 |
| **取值范围** | `0` 到 `4,294,967,295` | `-2,147,483,648` 到 `2,147,483,647` |
| **可表示负数** | ❌ 不可以 | ✅ 可以 |
| **最大值** | 42亿 | 21亿 |
| **典型用途** | 数量、计数、ID | 差分、变化量、相对值 |

---

## 💡 类型选择规则

### ✅ 使用 UInt32 的场景

**判断标准**：数值**永远 ≥ 0**

```rust
// 数量类字段
volume: UInt32              // 成交量（0或正整数）
open_interest: UInt32       // 持仓量（0或正整数）
bid_volume_1: UInt32        // 买一量（0或正整数）
ask_volume_1: UInt32        // 卖一量（0或正整数）

// 其他永远非负的字段
user_id: UInt32             // 用户ID
contract_id: UInt32         // 合约ID
order_count: UInt32         // 订单数量
```

**优点**：
- 语义明确（告诉读者"这个值永远非负"）
- 存储范围更大（0 到 42亿 vs -21亿 到 +21亿）
- 类型安全（防止意外出现负数）

---

### ✅ 使用 Int32 的场景

**判断标准**：数值**可能 < 0**

```rust
// 差分类字段（最常见）
open_interest_diff: Int32   // 持仓变化 = 当前OI - 前一OI（可正可负）
volume_diff: Int32          // 成交量变化
price_change: Int32         // 价格变动（跳数）

// 其他可能为负的字段
profit_loss: Int32          // 盈亏（可正可负）
temperature: Int32          // 温度（可能低于0°C）
altitude: Int32             // 海拔（可能低于海平面）
account_balance_change: Int32  // 账户余额变化
```

**优点**：
- 可以表示负数
- 避免减法溢出问题
- 符合业务语义

---

## 📈 实际数据验证

### IF1502 合约的持仓变化数据

```python
文件: IF1502_1min.parquet

Schema:
  open_interest:      uint32  ✅ 持仓量永远≥0
  open_interest_diff: int32   ✅ 持仓变化可正可负
  volume:             uint32  ✅ 成交量永远≥0

数据分布（共9,922个bar）:
  正数（增仓）: 5,550 次 (55.9%)
  负数（减仓）: 4,230 次 (42.6%)  ← 证明必须用 Int32！
  零值（不变）: 142 次   (1.4%)

  最小值: -11,098 手  ← UInt32无法表示！
  最大值: +5,782 手
```

**如果错用 UInt32 会发生什么？**
```rust
// 场景：持仓从 25,500 减少到 25,200
let diff: u32 = 25200_u32 - 25500_u32;
// ❌ 错误！结果 = 4,294,956,998（溢出成巨大正数）

let diff: i32 = 25200_i32 - 25500_i32;
// ✅ 正确！结果 = -300
```

---

## 🔍 代码中的类型转换逻辑

### parquet_processor_on_batch.rs 中的实现

```rust
// 第440-443行：open_interest_diff 计算
(col("last_open_interest").cast(DataType::Int64)
 - col("prev_open_interest").cast(DataType::Int64))
    .fill_null(lit(0))
    .cast(DataType::Int32)
    .alias("open_interest_diff"),
```

**转换链解析**：

```
Step 1: UInt32 → Int64
  原因：避免中间计算溢出
  例如：如果 open_interest 接近 21亿，直接转 Int32 会溢出

Step 2: Int64 减法
  last_open_interest:  25,200 (Int64)
  prev_open_interest:  25,500 (Int64)
  结果:                -300   (Int64)

Step 3: Int64 → Int32
  -300 (Int64) → -300 (Int32)
  原因：最终输出使用 Int32（范围足够，节省空间）
```

**为什么不直接用 Int32？**
```rust
// ❌ 危险：大数值转换可能溢出
let oi: u32 = 3_000_000_000;  // 30亿
let oi_i32: i32 = oi as i32;  // ❌ 溢出！变成负数

// ✅ 安全：先转 Int64 再计算
let oi: u32 = 3_000_000_000;
let oi_i64: i64 = oi as i64;  // ✅ 30亿在Int64范围内
```

---

## 📋 完整字段类型设计表

| 字段 | 类型 | 原因 | 典型范围 | NULL? |
|------|------|------|---------|-------|
| **contract** | String | 文本 | IF1706 | ❌ |
| **contract_yymm** | String | 文本 | 1706 | ❌ |
| **comd** | String | 文本 | IF | ❌ |
| **exchange** | String | 文本 | CFFEX | ❌ |
| **date** | String | 日期 | 2017-01-03 | ❌ |
| **trade_time** | String | 时间 | 2017-01-03 09:30:00 | ❌ |
| **open** | Float64 | 价格 | 3505.5 | ❌ |
| **high** | Float64 | 价格 | 3510.0 | ❌ |
| **low** | Float64 | 价格 | 3505.0 | ❌ |
| **close** | Float64 | 价格 | 3505.5 | ❌ |
| **prev_close** | Float64 | 价格（可无前值） | 3500.0 | ✅ |
| **pre_settle** | Float64 | 价格（补全bar无） | 3500.0 | ✅ |
| **volume** | UInt32 | 数量≥0 | 0-1,000,000 | ❌ |
| **turnover** | Float64 | 金额≥0 | 0.0-1e12 | ❌ |
| **open_interest** | UInt32 | 持仓量≥0 | 0-10,000,000 | ❌ |
| **open_interest_diff** | **Int32** | **差分可负** | **-11,098 到 +5,782** | ❌ |
| **bid_price_1** | Float64 | 价格 | 3505.5 | ❌ |
| **bid_volume_1** | UInt32 | 数量≥0 | 0-100,000 | ❌ |
| **ask_price_1** | Float64 | 价格 | 3506.0 | ❌ |
| **ask_volume_1** | UInt32 | 数量≥0 | 0-100,000 | ❌ |
| **mid_price** | Float64 | 价格 | 3505.75 | ❌ |
| **vwap** | Float64 | 价格 | 3505.5 | ❌ |
| **log_return** | Float64 | 收益率（可无前值） | -0.1 到 +0.1 | ✅ |
| **maturity_month** | Int16 | 相对月数可负 | -12 到 +24 | ❌ |
| **maturity_day** | Int16 | 相对天数可负 | -365 到 +730 | ❌ |

---

## 🎓 类型选择的最佳实践

### 1️⃣ **优先使用无符号类型（UInt）**

**当且仅当**数值在业务逻辑上永远非负时：

```rust
✅ 好的设计
struct Order {
    order_id: u64,        // ID永远≥0
    quantity: u32,        // 数量永远≥0
    price: f64,           // 价格永远≥0
}

❌ 差的设计
struct Order {
    order_id: i64,        // 浪费了一半空间（负数ID无意义）
    quantity: i32,        // 可能出现负数量？不合理
}
```

### 2️⃣ **差分/变化量必须用有符号类型（Int）**

```rust
✅ 正确
struct BarMetrics {
    open_interest: u32,         // 当前持仓量（≥0）
    open_interest_diff: i32,    // 持仓变化（可正可负）
    volume: u32,                // 当前成交量（≥0）
    volume_change: i32,         // 成交量变化（可正可负）
}

❌ 错误
struct BarMetrics {
    open_interest: u32,
    open_interest_diff: u32,    // ❌ 无法表示持仓减少！
}
```

### 3️⃣ **选择合适的位宽**

```rust
// 根据实际数值范围选择
maturity_month: i16,     // -12 到 +24，i16足够（节省空间）
open_interest: u32,      // 0 到 1000万，u32足够
user_id: u64,            // 可能数十亿用户，需要u64
price: f64,              // 价格需要精度，用f64
```

---

## 🔧 类型转换安全指南

### ✅ 安全的转换

```rust
// 小范围 → 大范围（无损）
let x: u32 = 1000;
let y: u64 = x as u64;  // ✅ 安全

// 有符号小范围 → 有符号大范围（无损）
let x: i32 = -300;
let y: i64 = x as i64;  // ✅ 安全
```

### ⚠️ 危险的转换

```rust
// 无符号 → 有符号（可能溢出）
let x: u32 = 3_000_000_000;  // 30亿
let y: i32 = x as i32;       // ❌ 溢出！变成负数
let y: i64 = x as i64;       // ✅ 安全

// 大范围 → 小范围（可能截断）
let x: i64 = 3_000_000_000;
let y: i32 = x as i32;       // ❌ 溢出！
```

### ✅ 推荐做法

```rust
// 方案1：先转大类型再计算
let a: u32 = 3_000_000_000;
let b: u32 = 2_000_000_000;
let diff = (a as i64) - (b as i64);  // ✅ 先转i64再减法
let result = diff as i32;            // ✅ 结果在i32范围内

// 方案2：使用checked操作
let a: u32 = 100;
let b: u32 = 200;
let diff = a.checked_sub(b);  // ✅ 返回Option<u32>，None表示溢出
```

---

## 📚 相关文档

- `parquet输出类型.md` - 完整输出格式说明
- `输出格式快速参考.md` - 快速查询表
- `src/parquet_processor_on_batch.rs:440-443` - open_interest_diff计算代码

---

## ❓ 常见问题

### Q1: 为什么不全部用 Int64，这样最安全？

**A**:
- ❌ 浪费空间：Int64 占8字节，Int32 占4字节
- ❌ 浪费带宽：网络传输和磁盘IO都会增加
- ❌ 降低性能：内存占用增加，缓存效率降低
- ✅ 合适就好：Int32 的 ±21亿范围足够覆盖99.99%的场景

### Q2: maturity_month 为什么不用 UInt16？

**A**:
已过期合约的到期月数是**负数**：
```
当前日期: 2025-05-01
合约到期: 2025-03-15
maturity_month = -2  ← 负数，UInt16无法表示
```

### Q3: 能否用 Float64 代替 Int32 来存储 open_interest_diff？

**A**:
❌ 不推荐：
- 浪费空间（8字节 vs 4字节）
- 精度问题（超过 2^53 可能丢失精度）
- 语义不清（持仓变化是整数，不应该有小数）

---

## ✅ 总结

| 类型选择 | 关键判断 | 典型字段 |
|---------|---------|---------|
| **UInt32** | 数值永远 ≥ 0 | volume, open_interest |
| **Int32** | 数值可能 < 0 | open_interest_diff |
| **Int16** | 小范围可负值 | maturity_month, maturity_day |
| **Float64** | 需要小数精度 | price, vwap, log_return |

**黄金法则**：
1. 能用 UInt 就用 UInt（明确语义 + 更大范围）
2. 差分/变化量必须用 Int（可正可负）
3. 选择合适位宽（避免浪费空间）
4. 类型转换时先放大再计算（避免溢出）
