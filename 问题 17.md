 ✅ 目录重组和对比测试完成

  已成功完成所有任务：

  📝 完成的修改

  1. 更新 parquet_processor_on_tick.rs 输出路径:
    - 1min输出: /data/future_data/dongzheng_data/full_bar_1min_on_tick/{contract}_1min.parquet
    - 5min输出: /data/future_data/dongzheng_data/full_bar_5min_on_tick/{contract}_5min.parquet
    - 移除了文件名中的 _on_tick 后缀（目录已区分）
  2. 更新 process_and_compare_contracts.py 对比脚本:
    - 修改为从两个独立目录读取数据
    - 默认只处理2501合约（可通过 --patterns 参数修改）
  3. 创建新目录结构:
    - /data/future_data/dongzheng_data/full_bar_1min/ (on_batch输出)
    - /data/future_data/dongzheng_data/full_bar_1min_on_tick/ (on_tick输出)

  📊 2501合约对比测试结果

  处理统计:
  - ✅ 找到73个2501合约（覆盖73个品种）
  - ✅ on_batch: 73/73成功处理
  - ✅ on_tick: 73/73成功处理

  对比结果:
  - Bars数量一致: 71/73 (97.3%) ✅
  - 收盘时间点一致: 72/72 (100%) ✅
  - 集合竞价一致: 49/49 (100%) ✅

  不一致的合约:
  - ❌ bb2501: batch=37,777, tick=37,246, 差异=531
  - ❌ wr2501: batch=50,306, tick=49,775, 差异=531

  🔍 分析

  1. PK2501已修复 ✅:
    - 之前的跨天flush bug已解决
    - PK2501现在完全一致: 54,993 = 54,993
  2. bb2501和wr2501差异:
    - 两个合约都是exactly 531条差异
    - 说明是系统性问题，可能与 fill_missing_minutes 逻辑有关
    - 这与之前提到的"empty bar differences"一致
  3. 整体评估:
    - 跨天flush修复非常成功
    - 收盘tick处理100%一致
    - 集合竞价处理100%一致
    - 仅2个合约存在填充空bar的差异

  📂 新的目录结构

  /data/future_data/dongzheng_data/
  ├── full_tick/              # 原始tick数据
  ├── full_bar_1min/          # on_batch处理结果 (批量向量化)
  ├── full_bar_1min_on_tick/  # on_tick处理结果 (逐tick流式)
  └── full_bar_5min/          # 5min聚合结果

  🎯 建议

  bb2501和wr2501的531条差异可能需要进一步调查，但这很可能是 fill_missing_minutes
  函数在处理最后交易日或特殊情况时的差异。整体来看，核心的tick聚合逻辑已经完全一致（收盘和集合竞价都是100%匹配）。
