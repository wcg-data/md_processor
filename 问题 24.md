 ✅ 好消息（核心数据完美）

  1. Bar数量100%一致: 所有8656个合约的bar数量和时间点完全一致
  2. 核心交易数据完美: open/high/low/close/volume/turnover/open_interest等字段的数值完全一致
  3. 集合竞价处理正确: on_batch和on_tick的集合竞价bar生成逻辑一致

  ⚠️ 需要关注（null处理差异）

  发现3个关键差异，都是null位置不一致：

  1. pre_settle字段 (影响~98%合约):
    - on_batch: 全部null ✅ 正确
    - on_tick: 全部0.0 ❌ Bug!
    - 原因: get_f64()把null转成了0.0而不是NaN
  2. prev_close字段 (影响~89%合约):
    - on_batch: 2个null
    - on_tick: 507个null
    - 原因: 需要进一步调查
  3. log_return字段: 依赖prev_close计算，所以null数量也不同

  🔧 根本原因

  在 src/parquet_processor_on_tick.rs:84-86 和 :31:

  // Bug #1: 读取时null→0.0
  let get_f64 = |idx| match row.0[idx] {
      AnyValue::Float64(v) => v,
      _ => 0.0,  // ← 应该: AnyValue::Null => f64::NAN
  };

  // Bug #2: 输出时未处理NaN→None
  Series::new("pre_settle", bars.iter().map(|b| b.pre_settle).collect::<Vec<_>>()),
  // 应该像prev_close一样处理NaN

  📝 已生成的文档

  1. 深度对比报告: on_batch_vs_on_tick_深度对比报告.md
    - 详细的差异分析
    - 根本原因定位
    - 修复建议和优先级
    - 业务影响评估
  2. 对比脚本:
    - script/compare_batch_vs_tick.py - 快速对比bar数量
    - script/deep_compare_batch_tick.py - 深度对比所有字段+null
    - script/process_all_on_tick.sh - 并行处理on_tick
    - script/run_full_comparison.sh - 一键完整流程

  🎯 推荐下一步

  您可以选择：

  1. 接受当前结果 - 核心数据完美一致，null差异不影响交易
  2. 修复null处理 - 我可以帮您修复get_f64函数，达到完美一致
  3. 调查prev_close差异 - 深入分析为什么null数量不同