 let trade_time = self.current_bar_minute
      .unwrap_or_else(|| Utc::now())
      .with_timezone(&*UTC_PLUS_8)  // ❌ 错误的时区转换！
      .format("%Y-%m-%d %H:%M:%S")
      .to_string();

  floor_to_1min返回的是UTC时间，但这里又转换成了UTC+8，导致时间增加8小时！