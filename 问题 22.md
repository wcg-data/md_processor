  我觉得 on batch 同样的情况也应该变成 nan, 修改完之后你再看看是否还有别的环节 不一致 

  
 📋 on_tick vs on_batch Validate对比

  ✅ on_tick (validate_tick函数 - bar1min_aggregator.rs:446)

  关键规则：

  // 1. close价格检查
  if tick.close <= 0.0 || !tick.close.is_finite() { return false; }

  // 2. 基础字段非空
  if tick.comd[0] == 0 { return false; }
  if tick.contract[0] == 0 { return false; }
  if tick.trade_time[0] == 0 { return false; }

  // 3. turnover非负
  if tick.turnover < 0.0 { return false; }

  // 4. bid/ask检查 - 允许0，但拒绝负数和Infinity
  if tick.bid_price_1 < 0.0 || tick.bid_price_1.is_infinite() { return false; }
  if tick.ask_price_1 < 0.0 || tick.ask_price_1.is_infinite() { return false; }

  // 5. bid <= ask (仅当两者都>0时)
  if tick.bid_price_1 > 0.0 && tick.ask_price_1 > 0.0 && tick.bid_price_1 > tick.ask_price_1 {
      return false;
  }

  // 6. 清理bid/ask=0的情况（⚠️ 修改tick内容）
  if tick.bid_price_1 == 0.0 || tick.bid_price_1.is_nan() {
      tick.bid_price_1 = f64::NAN;  // 0 → NaN
      tick.bid_volume_1 = 0;
  }
  if tick.ask_price_1 == 0.0 || tick.ask_price_1.is_nan() {
      tick.ask_price_1 = f64::NAN;  // 0 → NaN
      tick.ask_volume_1 = 0;
  }

  // 7. 重新计算mid_price
  if tick.bid_price_1.is_nan() || tick.ask_price_1.is_nan() {
      tick.mid_price = f64::NAN;
  } else {
      tick.mid_price = (tick.bid_price_1 + tick.ask_price_1) / 2.0;
  }

  // 8. volume/turnover一致性
  if !(tick.volume == 0 && (tick.turnover == 0.0 || tick.turnover.is_nan())
      || (tick.volume > 0 && tick.turnover > 0.0)) {
      return false;
  }

  // 9. 交易时段检查（包括集合竞价时段）
  // ... 检查是否在trading_ranges或auction_periods内

  ---
  ✅ on_batch (向量化过滤 - parquet_processor_on_batch.rs:80)

  关键规则：

  .filter(
      col("close").is_not_null()
          .and(col("close").gt(lit(0.0)))  // close > 0
          .and(col("volume").is_not_null())
          .and(col("turnover").is_not_null())
          .and(col("turnover").gt_eq(lit(0.0)))  // turnover >= 0
          .and(col("bid_price_1").is_not_null())
          .and(col("bid_price_1").gt_eq(lit(0.0)))  // bid >= 0 (允许0)
          .and((col("bid_price_1").is_nan()).or(col("bid_price_1").is_finite()))  // 允许NaN，拒绝Infinity
          .and(col("ask_price_1").is_not_null())
          .and(col("ask_price_1").gt_eq(lit(0.0)))  // ask >= 0 (允许0)
          .and((col("ask_price_1").is_nan()).or(col("ask_price_1").is_finite()))  // 允许NaN，拒绝Infinity
          // bid <= ask 检查：只在bid和ask都>0时才要求bid<=ask
          .and(
              col("bid_price_1").eq(lit(0.0))  // bid=0, 通过
              .or(col("ask_price_1").eq(lit(0.0)))  // ask=0, 通过
              .or(col("bid_price_1").lt_eq(col("ask_price_1")))  // bid>0且ask>0时bid<=ask
          )
          // volume和turnover一致性
          .and(
              (col("volume").eq(lit(0)).and(
                  col("turnover").eq(lit(0.0)).or(col("turnover").is_nan())
              ))
              .or(col("volume").gt(lit(0)).and(col("turnover").gt(lit(0.0))))
          )
          // 字段非空
          .and(col("comd").is_not_null())
          .and(col("contract").is_not_null())
          .and(col("trade_time").is_not_null())
  )

  // 然后交易时段过滤（包括auction_periods）
  for i in 0..result_df.height() {
      // 检查是否在trading_ranges或auction_periods内
      let is_in_trading = trading_ranges.iter().any(...);
      let is_in_auction = auction_periods.iter().any(...);
      if is_in_trading || is_in_auction {
          valid_indices.push(i);
      }
  }

  ---
  🔍 关键差异分析

  | 规则                 | on_tick | on_batch | 差异     |
  |--------------------|---------|----------|--------|
  | bid/ask=0允许？       | ✅ 允许    | ✅ 允许     | 一致     |
  | bid/ask<0过滤？       | ✅ 过滤    | ✅ 过滤     | 一致     |
  | bid/ask清理？         | ✅ 0→NaN | ❌ 保持原值   | ⚠️ 差异！ |
  | mid_price重算？       | ✅ 重新计算  | ❌ 使用原值   | ⚠️ 差异！ |
  | volume/turnover一致性 | ✅ 检查    | ✅ 检查     | 一致     |
  | 交易时段过滤             | ✅ 是     | ✅ 是      | 一致     |

  ---
  ⚠️ 根本差异：bid/ask=0的处理

  au2409在2024-09-10的20:59:00.500 tick：
  - bid_price_1 = 0.0
  - ask_price_1 = 0.0
  - mid_price = 0.0

  on_tick处理后：
  validate_tick() → 返回true (允许通过)
  但是tick被修改了：
    bid_price_1 = NaN  // 0.0 → NaN
    ask_price_1 = NaN  // 0.0 → NaN
    mid_price = NaN    // 重新计算

  on_batch处理后：
  filter() → 通过 (允许bid=0, ask=0)
  tick保持原值：
    bid_price_1 = 0.0  // 保持不变
    ask_price_1 = 0.0  // 保持不变
    mid_price = 0.0    // 保持不变

  ---
  🎯 结论

  两边的过滤规则在表面上是一致的（都允许bid=0,ask=0通过），但存在一个关键差异：

  1. on_tick: validate_tick虽然返回true，但会修改tick内容（0→NaN）
  2. on_batch: filter只判断是否通过，不修改tick内容（0保持为0）

  这个差异导致：
  - ✅ on_tick: bid/ask被清理为NaN后，数据更干净
  - ⚠️ on_batch: bid/ask保持为0，可能在后续处理中被错误使用

  但这个差异不能解释为什么on_tick没有生成21:00
  bar。因为即使bid/ask被清理为NaN，这个tick仍然应该被放入auction_ticks，文件结束时仍然应该生成21:00 bar。

  让我检查一下是否还有其他环节...

> 我觉得 on batch 同样的情况也应该变成 nan, 修改完之后你再看看是否还有别的环节 不一致 

● 你说得对！on_batch应该和on_tick保持完全一致，也要将bid/ask=0转换为NaN并重新计算mid_price。