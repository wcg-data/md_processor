# on_batch 过滤规则严格化修复报告

**修复日期**: 2025-11-02
**修复版本**: v1.5
**修复文件**: `src/parquet_processor_on_batch.rs`

---

## 1. 问题背景

在完整对比 on_batch 和 on_tick 两种处理器的输出结果时，发现：

**修复前统计**：
- 总合约数: 8,703
- Bar数量一致: 8,087 (92%)
- **Bar数量差异: 616 合约 (8%)**

**典型问题合约**：
| 合约 | on_batch | on_tick | 差异 | 差异率 |
|------|----------|---------|------|--------|
| SR1109 | 84,056 | 2,174 | 81,882 | 97.4% |
| SR1201 | 83,624 | 3,496 | 80,128 | 95.8% |
| WS1201 | 45,556 | 1,396 | 44,160 | 96.9% |
| CF1205 | 37,549 | 32,903 | 4,646 | 12.4% |

---

## 2. 根本原因分析

### 2.1 过滤规则不一致

**on_batch 过滤逻辑**（修复前，line 102）：
```rust
c > 0.0 && t >= 0.0 && b >= 0.0 && a >= 0.0
```
- ❌ 允许 `volume > 0 && turnover = 0` 的数据通过

**on_tick 过滤逻辑**（`bar1min_aggregator.rs`, lines 547-550）：
```rust
if !(tick.volume == 0 && (tick.turnover == 0.0 || tick.turnover.is_nan())
    || (tick.volume > 0 && tick.turnover > 0.0)) {
    return false;
}
```
- ✅ 严格要求：
  - `volume = 0` 时，`turnover` 必须为 `0` 或 `NaN`
  - `volume > 0` 时，`turnover` 必须 `> 0`

### 2.2 历史数据质量问题

**问题数据分布**：
- 时间范围: 2010年 ~ 2011年5月
- 交易所: 主要是郑商所（ZCE）
- 品种: SR、WS、CF 等
- 特征: **成交量 > 0，但成交额 = 0**（数据质量问题）

**示例**：SR1109 合约
- 原始 tick 数据: 5,900,000 条
- on_batch 处理: 84,056 bars（包含2010-2011年5月的问题数据）
- on_tick 处理: 2,174 bars（仅处理2011-05-19之后的有效数据）
- **差异**: 97.4% 的 bar 是由数据质量问题导致的

---

## 3. 修复方案

### 3.1 修改内容

**文件**: `src/parquet_processor_on_batch.rs`
**位置**: Lines 100-110

**修复前**：
```rust
let is_valid = match (close_val, volume_val, turnover_val, bid_val, ask_val) {
    (AnyValue::Float64(c), _, AnyValue::Float64(t), AnyValue::Float64(b), AnyValue::Float64(a)) => {
        c > 0.0 && t >= 0.0 && b >= 0.0 && a >= 0.0
        && (b.is_finite() || b.is_nan()) && (a.is_finite() || a.is_nan())
        && (b == 0.0 || a == 0.0 || b <= a)
    },
    _ => false,
};
```

**修复后**：
```rust
let is_valid = match (close_val, volume_val, turnover_val, bid_val, ask_val) {
    (AnyValue::Float64(c), AnyValue::UInt32(v), AnyValue::Float64(t), AnyValue::Float64(b), AnyValue::Float64(a)) => {
        c > 0.0 && t >= 0.0 && b >= 0.0 && a >= 0.0
        && (b.is_finite() || b.is_nan()) && (a.is_finite() || a.is_nan())
        && (b == 0.0 || a == 0.0 || b <= a)
        // 添加严格的 volume-turnover 一致性检查（与 on_tick 保持一致）
        // 要求：volume=0时turnover必须为0或NaN；volume>0时turnover必须>0
        && ((v == 0 && (t == 0.0 || t.is_nan())) || (v > 0 && t > 0.0))
    },
    _ => false,
};
```

### 3.2 修复原则

1. **严格一致性**: on_batch 和 on_tick 使用完全相同的过滤规则
2. **数据质量优先**: 拒绝 `volume>0 && turnover=0` 的异常数据
3. **向后兼容**: 不影响正常的历史数据处理

---

## 4. 修复结果验证

### 4.1 单合约测试

**SR1109**：
- 修复前: on_batch=84,056, on_tick=2,174, 差异=81,882 (97.4%)
- 修复后: on_batch=2,174, on_tick=2,174, **差异=0** ✅

**SR1201**：
- 修复前: on_batch=83,624, on_tick=3,496, 差异=80,128 (95.8%)
- 修复后: on_batch=3,496, on_tick=3,496, **差异=0** ✅

### 4.2 全量对比测试

运行完整对比流程（8,703 个合约）：

```bash
./script/run_full_comparison.sh
```

**测试结果**：

| 指标 | 修复前 | 修复后 | 改进 |
|------|--------|--------|------|
| 总合约数 | 8,703 | 8,656 | -47 (空数据) |
| **Bar数量一致** | 8,087 (92%) | **8,656 (100%)** | **+8%** |
| Bar数量差异 | 616 (8%) | **0 (0%)** | **-100%** |
| 时间戳一致 | 8,087 | **8,656** | ✅ |
| 成交量一致 | 8,087 | **8,656** | ✅ |
| 成交额一致 | 8,087 | **8,656** | ✅ |
| 持仓量一致 | 8,087 | **8,656** | ✅ |
| **完全一致** | 8,087 (92%) | **8,656 (100%)** | **+8%** |

**结论**: ✅ **on_batch 和 on_tick 现在完全一致！**

### 4.3 性能影响

- 编译时间: 无变化
- 处理速度: 无显著影响（过滤器仅增加一个简单的条件判断）
- 内存使用: 无变化

---

## 5. 数据影响分析

### 5.1 被过滤的数据

**时间范围**: 2010年 ~ 2011年5月
**交易所**: 主要是郑商所（ZCE）
**品种**: SR、WS、CF 等
**数据量**: 约 616 个合约受影响，过滤掉的 bar 数量从几千到几万不等

### 5.2 数据质量评估

这些被过滤的数据具有明显的质量问题：
- **成交量 > 0，但成交额 = 0**（逻辑上不可能）
- 主要集中在早期历史数据（2010-2011年）
- 可能是数据源的采集或处理问题

**过滤决策**: ✅ **正确**
- 这些数据会导致下游分析错误（如价格计算、收益率分析等）
- 严格过滤提高了数据质量和一致性

---

## 6. 后续建议

### 6.1 文档更新

- [x] 更新 `docs/validate_tick_过滤规则.md`，添加此修复说明
- [x] 更新版本号到 v1.5
- [x] 在 CLAUDE.md 中记录此次修复

### 6.2 代码维护

1. **保持一致性**: 未来修改过滤规则时，必须同时更新：
   - `bar1min_aggregator.rs` 的 `validate_tick` 函数
   - `parquet_processor_on_batch.rs` 的过滤逻辑

2. **测试覆盖**: 每次修改后运行完整对比测试：
   ```bash
   ./script/run_full_comparison.sh
   ```

3. **回归测试**: 保留关键合约的测试用例（SR1109、SR1201 等）

---

## 7. 总结

### 7.1 成果

✅ **完全消除了 on_batch 和 on_tick 之间的差异**
- Bar数量: 100% 一致（8,656/8,656）
- 时间戳: 100% 一致
- 成交量: 100% 一致
- 成交额: 100% 一致
- 持仓量: 100% 一致

### 7.2 意义

1. **数据一致性**: on_batch 和 on_tick 现在可以互换使用，结果完全相同
2. **数据质量**: 严格过滤提高了输出数据的质量和可靠性
3. **维护性**: 统一的过滤规则降低了维护成本和出错风险

### 7.3 关键经验

1. **过滤规则一致性至关重要**: 不同处理路径必须使用相同的验证逻辑
2. **数据质量优先**: 对于明显错误的数据，应该严格拒绝而不是宽容处理
3. **全量测试必不可少**: 单合约测试无法发现系统性问题

---

**修复完成** ✅
**测试通过** ✅
**文档更新** ✅
