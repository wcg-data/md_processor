
# 问题17的真正根因

## 发现的新问题

修复flush()后，bb2501 2025-01-02的bars出现数据错误：
- 原始tick: close=200.0, oi=2
- 生成的bars: close=95.45, oi=0

95.45是2025-01-10的价格，说明集合竞价bar使用了错误的tick数据。

## 根本原因

`auction_ticks`缓冲区在处理多个日期时会混合不同日期的tick：

```rust
pub struct Bar1MinAggregator {
    auction_ticks: Vec<TickData>,  // ← 问题：跨日期累积
    in_auction_period: bool,
    // ...
}
```

**处理流程（错误）**：
1. 2025-01-02 08:59:00 tick → 加入auction_ticks, in_auction_period=true
2. 2025-01-02 15:02:43 tick → 被validate_tick过滤（不在交易时段）
3. **没有连续交易tick触发flush_auction_bar()**
4. 2025-01-03 08:59:00 tick → 继续加入auction_ticks（❌ 混合了两天的数据）
5. ...
6. 2025-01-10 08:59:00 tick → 继续加入auction_ticks
7. 文件结束 → flush() → flush_auction_bar()
8. ❌ **flush_auction_bar()使用auction_ticks.last()（2025-01-10的tick）**
9. 结果：所有只有集合竞价tick的日期，都使用最后一天的数据生成bar

## 修复方案

在on_tick()中，检测到跨天时，必须先flush之前的集合竞价bar：

```rust
pub fn on_tick(&mut self, tick: &TickData) -> Option<BarData> {
    let tick_naive = tick_time.naive_utc();
    
    // 【新增】检查是否跨天（即使在集合竞价时段内）
    if self.in_auction_period {
        let last_auction_date = to_string_field(&self.auction_ticks.last().unwrap().date);
        let curr_date = to_string_field(&tick.date);
        
        if last_auction_date != curr_date {
            // 跨天：flush前一天的集合竞价bar
            let auction_bar = self.flush_auction_bar();
            self.in_auction_period = false;
            self.auction_opening_time = None;
            
            // 继续处理当前tick...
            return auction_bar;
        }
    }
    
    // 原有逻辑...
}
```

