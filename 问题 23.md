这是一个跨集合竞价时段的bug：

  Bug的完整链条

  1. Tick #4 (15:00:00.500) 被validate_tick拒绝 - 超出交易时段[13:30:00, 15:00:00)
  2. Tick #3 (08:59:00.500) 进入集合竞价后，in_auction_period保持true - 因为没有连续交易tick触发离开集合竞价
  3. Tick #5 (20:59:00.500) 到来时：
    - 检测到in_auction_period=true，进入"已经在集合竞价时段内"的逻辑（bar1min_aggregator.rs:61-90）
    - 检查是否跨天：两个tick的date字段都是"2024-09-10"→ 不跨天
    - 直接累积：auction_ticks.push(20:59:00.500)
    - ⚠️ auction_opening_time没有更新，仍然是09:00
  4. 文件结束时调用flush()：
    - flush_auction_bar()使用auction_opening_time=09:00生成bar
    - 结果：生成了09:00 bar（open=570.50, close=572.20）
    - 没有生成21:00 bar！

  核心问题

  跨天检查只检查date字段，但date字段是交易日，不能区分同一交易日的不同集合竞价时段（09:00和21:00）。

  bar1min_aggregator.rs:66-84的逻辑：
  if !self.auction_ticks.is_empty() {
      let last_auction_date = to_string_field(&self.auction_ticks.last().unwrap().date);
      let curr_date = to_string_field(&tick.date);

      if last_auction_date != curr_date {  // ⚠️ 只检查date，不检查时段
          // 跨天：flush前一天的集合竞价bar
          let prev_auction_bar = self.flush_auction_bar();
          // ...
      }
  }

  为什么on_batch没有这个问题

  on_batch在处理集合竞价时（parquet_processor_on_batch.rs:184-206），会为每个集合竞价时段独立分组，不会混在一起。

  现在的差异总结

  | 合约                | on_batch              | on_tick               | 差异原因                                        |
  |-------------------|-----------------------|-----------------------|---------------------------------------------|
  | au2409 2024-09-10 | 407 bars (有21:00 bar) | 227 bars (无21:00 bar) | on_tick将08:59和20:59两个集合竞价tick混合生成了09:00 
  bar |

  你要我修复这个bug吗？修复方法是：在bar1min_aggregator.rs中，跨时段检查不仅要检查date字段，还要检查当前tick的集合竞价时段是否
  与auction_opening_time对应的时段相同。
