# 问题 23：on_tick跨集合竞价时段bug修复

## 问题发现

2025-10-30，在对比on_batch和on_tick的230个核心合约结果时，发现仅2个合约存在差异：
- **au2409**：on_batch 407 bars，on_tick 227 bars
- **bc2509**：on_batch 110579 bars，on_tick 110579 bars（后来修复后一致）

## 根本原因

### Bug描述

当同一交易日有**多个集合竞价时段**（如日盘09:00和夜盘21:00）时，如果中间没有连续交易tick，on_tick会将不同时段的集合竞价tick混合在一起，导致：
1. 只生成第一个集合竞价时段的bar（如09:00）
2. 第二个集合竞价时段的bar丢失（如21:00）
3. 生成的bar数据错误（混合了两个时段的tick）

### 触发条件

1. 品种有多个集合竞价时段（如au有日盘08:55-09:00和夜盘20:55-21:00）
2. 异常交易日，只有集合竞价tick，中间缺少连续交易tick
3. 两个集合竞价tick的**date字段相同**（都是同一交易日）

### 实际案例：au2409 2024-09-10

**原始tick数据**（仅5个tick）：
```
Tick #1: 00:59:57.500 (前一天夜盘连续交易) → date=2024-09-10
Tick #2: 02:28:59.000 (夜盘收盘后) → 被validate_tick拒绝
Tick #3: 08:59:00.500 (日盘集合竞价) → date=2024-09-10, close=570.50
Tick #4: 15:00:00.500 (日盘收盘后) → 被validate_tick拒绝
Tick #5: 20:59:00.500 (夜盘集合竞价) → date=2024-09-10, close=572.20
```

**Bug流程**：

1. Tick #1 (00:59:57.500)：连续交易tick
   - `last_tick` = Tick #1

2. Tick #3 (08:59:00.500)：日盘集合竞价
   - 进入集合竞价时段，`in_auction_period` = true
   - `auction_ticks` = [Tick #3]
   - `auction_opening_time` = 09:00
   - `last_tick` = None（flush了前一个bar）

3. Tick #5 (20:59:00.500)：夜盘集合竞价
   - 检测到`in_auction_period=true`，进入"已经在集合竞价时段内"的分支
   - **旧代码只检查date字段**：Tick #3和#5的date都是"2024-09-10" → 判定为同一天
   - **错误地继续累积**：`auction_ticks` = [Tick #3, Tick #5]
   - **⚠️ `auction_opening_time`没有更新，仍为09:00**

4. 文件结束，调用`flush()`：
   - `flush_auction_bar()`使用`auction_opening_time=09:00`生成bar
   - **结果**：只生成了09:00 bar（open=570.50, close=572.20）
   - **问题**：混合了两个时段的数据，21:00 bar丢失

### 集合竞价时段配置机制

系统通过两种方式确定集合竞价时段：

1. **明确配置**：CSV中配置了`auction_time`
   ```csv
   au,20:55:00,21:00:00,02:30:00  # 夜盘集合竞价明确配置
   ```

2. **自动推断**（trade_session_loader.rs:62-86）：
   ```rust
   - 09:00:00开盘 → 推断08:55-09:00为集合竞价（商品期货日盘）
   - 09:30:00开盘 → 推断09:25-09:30为集合竞价（股指期货）
   - 21:00:00开盘 → 推断20:55-21:00为集合竞价（夜盘）
   ```

   ```csv
   au,,09:00:00,10:15:00  # auction_time为空，自动推断08:55-09:00
   ```

因此au实际有**两个集合竞价时段**：
- 日盘：08:55-09:00（自动推断）
- 夜盘：20:55-21:00（明确配置）

## 修复方案

### 代码修改

**文件**：`src/bar1min_aggregator.rs`
**位置**：lines 61-112
**修复内容**：在"已经在集合竞价时段内"的分支中，**优先检查开盘时间是否相同**

```rust
} else {
    // 已经在集合竞价时段内
    // 【修复问题23】优先检查是否跨集合竞价时段
    // 场景：同一交易日有多个集合竞价时段（如au的09:00和21:00）
    // date字段相同（都是交易日），但开盘时间不同
    // 如果不检查，会将不同时段的auction_ticks混在一起，导致生成错误的bar
    if let Some(prev_opening_time) = self.auction_opening_time {
        if opening_time != prev_opening_time {
            // 跨集合竞价时段：flush前一个集合竞价bar
            let prev_auction_bar = self.flush_auction_bar();

            // 重置集合竞价状态，开始新的集合竞价时段
            self.in_auction_period = false;
            self.auction_opening_time = None;

            // 当前tick是新集合竞价时段的tick，重新进入集合竞价时段
            self.auction_ticks.push(*tick);
            self.in_auction_period = true;
            self.auction_opening_time = Some(opening_time);

            return prev_auction_bar;
        }
    }

    // 【修复问题17补充】检查是否跨天（原有逻辑，保留）
    if !self.auction_ticks.is_empty() {
        let last_auction_date = to_string_field(&self.auction_ticks.last().unwrap().date);
        let curr_date = to_string_field(&tick.date);

        if last_auction_date != curr_date {
            // 跨天：flush前一天的集合竞价bar
            ...
        }
    }

    // 同一天同一时段，继续累积
    self.auction_ticks.push(*tick);
    return None;
}
```

### 检查顺序

修复后的检查顺序（优先级从高到低）：
1. **跨集合竞价时段**：`opening_time != prev_opening_time` → flush
2. **跨天**：`date != prev_date` → flush
3. **同一天同一时段**：继续累积

## 修复验证

### au2409 2024-09-10

**修复前**：
```
on_tick: 227 bars
  - 09:00 bar: open=570.50, close=572.20 (⚠️ 混合了两个时段)
  - 21:00 bar: 无
```

**修复后**：
```
on_tick: 407 bars
  - 09:00 bar: open=570.50, close=570.50 ✓
  - 21:00 bar: open=572.20, close=572.20 ✓
on_batch: 407 bars ✓ 一致
```

### bc2509

**修复前**：存在bar数量差异
**修复后**：110579 bars，与on_batch完全一致 ✓

### 前20个合约测试

全部通过，0个差异 ✓

## 影响范围

### 受影响的品种

所有有**多个集合竞价时段**的品种，包括：
- 商品期货：有夜盘的品种（同时有日盘09:00和夜盘21:00集合竞价）
- 股指期货：开盘时间09:30（集合竞价09:25-09:30）

### 受影响的场景

- 异常交易日（节假日、停盘日）
- 只有集合竞价tick，缺少连续交易tick
- 极少数tick的合约（< 10 ticks/day）

### 正常交易日不受影响

正常交易日有连续交易tick，会在离开第一个集合竞价时段时自动flush，不会混合。

## 技术要点

1. **date字段是交易日，不是日历日期**
   - 2024-09-09 23:00的tick，date字段是"2024-09-10"
   - 同一交易日的不同时段，date字段相同

2. **opening_time是区分集合竞价时段的唯一标识**
   - 日盘集合竞价：opening_time = 09:00
   - 夜盘集合竞价：opening_time = 21:00

3. **检查顺序很重要**
   - 必须先检查开盘时间，再检查日期
   - 否则同一天的不同集合竞价会被混合

## 相关文档

- `集合竞价功能实现总结.md` - on_tick集合竞价原理
- `集合竞价修复测试报告.md` - on_batch集合竞价修复
- `问题 17.md` - 跨天集合竞价flush问题
- `问题 21.md` - 最后交易日停夜盘问题

## 修复时间

2025-10-30

## 状态

✅ 已修复并验证
