# 输出格式快速参考

**版本**: v2.0 (CSV已修复)
**更新日期**: 2025-11-01

---

## 📋 25个输出字段

```
1. contract           合约代码         (如: IF1706, rb2510, AP010)
2. contract_yymm      合约年月         (如: 1706, 2510, 0010)
3. comd               品种代码         (如: IF, rb, AP)
4. exchange           交易所           (CFFEX/SHFE/DCE/ZCE/INE/GFEX)
5. date               交易日期         (YYYY-MM-DD)
6. trade_time         交易时间         (YYYY-MM-DD HH:MM:SS)

7. open               开盘价
8. high               最高价
9. low                最低价
10. close             收盘价
11. prev_close        上一bar收盘价    ← 可为NULL
12. pre_settle        昨结算价         ← 可为NULL

13. volume            成交量
14. turnover          成交额
15. open_interest     持仓量
16. open_interest_diff 持仓量变化

17. bid_price_1       买一价           ← 清理后可为0/NULL
18. bid_volume_1      买一量           ← 清理后可为0/NULL
19. ask_price_1       卖一价           ← 清理后可为0/NULL
20. ask_volume_1      卖一量           ← 清理后可为0/NULL
21. mid_price         中间价           ← 清理后可为0/NULL

22. vwap              成交均价
23. log_return        对数收益率       ← 可为NULL

24. maturity_month    距到期月数
25. maturity_day      距到期天数
```

---

## 🔄 NULL值处理速查表（✅ 修复后）

| 字段 | 场景 | Parquet | CSV | 一致性 |
|------|------|---------|-----|--------|
| **prev_close** | 第一个bar | `NULL` | `""` | ✅ |
| **pre_settle** | 补全bar | `NULL` | `""` | ✅ |
| **log_return** | 第一个bar | `NULL` | `""` | ✅ |

**读取方式**:
```python
# Parquet: 自动识别NULL
df = pd.read_parquet('*.parquet')

# CSV: 需手动配置
df = pd.read_csv('*.csv', na_values=[''])
```

---

## 🧹 bid/ask清理规则速查

**触发条件**（validate_tick规则6）：
1. `bid_price_1 == 0.0 || bid_price_1.is_nan()` → 清理
2. `ask_price_1 == 0.0 || ask_price_1.is_nan()` → 清理
3. `bid_price_1 > 0 && ask_price_1 > 0 && bid > ask` → 清理

**清理结果**:
```
bid_price_1 = 0.0 (或NaN)
bid_volume_1 = 0
ask_price_1 = 0.0 (或NaN)
ask_volume_1 = 0
mid_price = 0.0
```

**输出差异**:
| 字段 | Parquet | CSV |
|------|---------|-----|
| bid_price_1 | `0.0` | `""` (空字符串) |
| bid_volume_1 | `0` | `""` (空字符串) |
| ask_price_1 | `0.0` | `""` (空字符串) |
| ask_volume_1 | `0` | `""` (空字符串) |
| mid_price | `0.0` | `""` (空字符串) |

---

## 📊 数据类型速查（Parquet）

```
String字段 (6):   contract, contract_yymm, comd, exchange, date, trade_time
Float64字段 (12): open, high, low, close, prev_close*, pre_settle*,
                  turnover, bid_price_1, ask_price_1, mid_price, vwap, log_return*
UInt32字段 (4):   volume, open_interest, bid_volume_1, ask_volume_1
                  ↑ 无符号（永远≥0）：数量类字段
Int32字段 (1):    open_interest_diff
                  ↑ 有符号（可正可负）：持仓变化 = 当前OI - 前一OI
Int16字段 (2):    maturity_month, maturity_day
                  ↑ 有符号（可正可负）：距到期时间（可能为负表示已过期）

* 可为NULL
```

---

## 🎯 典型场景示例

### 场景1: 第一个bar（集合竞价或开盘）
```python
# Parquet
{
  'prev_close': None,     # NULL
  'log_return': None,     # NULL
  'pre_settle': 3500.0,
  'volume': 150,
  ...
}

# CSV
IF1706,1706,IF,CFFEX,2017-01-03,2017-01-03 09:30:00,3505.5,3510.0,3505.0,3505.5,,3500.0,150,...
#                                                                              ↑空   ↑有值
```

### 场景2: 正常bar
```python
# Parquet
{
  'prev_close': 3505.5,
  'log_return': 0.000285,
  'bid_price_1': 3506.0,
  'ask_price_1': 3506.5,
  'volume': 80,
  ...
}

# CSV
IF1706,1706,IF,CFFEX,2017-01-03,2017-01-03 09:31:00,3505.5,3506.0,3505.0,3506.0,3505.5,3500.0,80,...,3506.0,18,3506.5,12,...,0.000285,...
```

### 场景3: 补全bar（无成交）
```python
# Parquet
{
  'prev_close': 3506.0,   # 延续价格
  'pre_settle': None,     # NULL（补全bar无昨结算）
  'bid_price_1': 0.0,     # 清理后
  'ask_price_1': 0.0,     # 清理后
  'volume': 0,
  'log_return': 0.0,      # 延续价格，收益率为0
  ...
}

# CSV
IF1706,1706,IF,CFFEX,2017-01-03,2017-01-03 09:32:00,3506.0,3506.0,3506.0,3506.0,3506.0,,0,0,25020,0,,,,,,,0.0,...
#                                                                              ↑有值↑空      ↑空字符串    ↑0.0
```

---

## 📁 输出路径

### Parquet
```bash
# 离线处理（on_tick模式）
/data/future_data/dongzheng_data/full_bar_1min_on_tick/{contract}_1min.parquet
/data/future_data/dongzheng_data/full_bar_5min_on_tick/{contract}_5min.parquet

# 离线处理（on_batch模式）
/data/future_data/dongzheng_data/full_bar_1min/{contract}_1min.parquet
/data/future_data/dongzheng_data/full_bar_5min/{contract}_5min.parquet
```

### CSV
```bash
# 实时处理（共享内存）
output/md_snapshot_RTdongzheng_1min.csv.20251027
output/md_snapshot_RTdongzheng_5min.csv.20251027
```

---

## ⚡ 性能对比

| 指标 | Parquet | CSV | 差异 |
|------|---------|-----|------|
| 文件大小 | 10MB | 100MB | **10x** |
| 读取速度 | 0.5s | 5s | **10x** |
| 写入速度 | 1s | 3s | **3x** |
| 类型安全 | ✅ | ❌ | - |
| NULL支持 | ✅ | ⚠️ | 需配置 |

**推荐**: 生产环境使用Parquet，调试使用CSV

---

## 🔧 代码生成位置

```rust
// Parquet输出
src/parquet_processor_on_tick.rs:17-50      (build_bars_dataframe)
src/parquet_processor_on_tick.rs:182-189    (pre_settle NaN转NULL)

// CSV输出
src/memory_processor_on_tick.rs:67-108     (write_bar_to_csv)
  - 第81-82行: prev_close, pre_settle       (✅ 修复)
  - 第88-92行: bid/ask/mid_price清理
  - 第94行: log_return处理
```

---

## 📖 相关文档

- `parquet输出类型.md` - 完整对比分析（9章节）
- `CSV输出格式修复说明.md` - 修复历史
- `CSV输出示例对比.md` - 修复前后示例
- `docs/validate_tick_过滤规则.md` - 数据清理规则
