有两个问题, 1.哪个更合理,专业的边界处理是怎么样的- on_batch 和 on_tick 的微小差异（<0.1%）来自：
    - 窗口边界tick的分配方式不同
    - prev tick的选择（如23:00 vs 22:59:59.500）
  - on_tick可能更准确：使用时间顺序上真正的最后一个tick   2. on_Batch 和 on_tick 难道用的不是同一种 日度累计清零的方法吗?  用的是 
组合使用吗?  let is_reset = volume_decreased || date_changed?  如果不是 为什么不是的话 为什么不直接跟 on_tick保持一致?有什么理由? 


 差异来源：
  - on_batch 的 prev: 22:59:59.500, vol=428,851 → 增量 = 698 ✅
  - on_tick  的 prev: 23:00:00,     vol=428,859 → 增量 = 690 ✅

  这不是bug！两者都正确，只是取的prev tick不同：
  - on_batch: 使用22:59窗口的最后tick（窗口聚合）
  - on_tick: 使用夜盘真实最后tick（包括23:00的tick）


1. on_batch 是正确的！

  - ✅ Volume和Turnover使用相同的重置检测逻辑
  - ✅ 使用 value < prev_value 检测重置
  - ✅ 重置时直接使用累积值，否则计算增量

  2. 微小差异是正常的

  - on_batch 和 on_tick 的微小差异（<0.1%）来自：
    - 窗口边界tick的分配方式不同
    - prev tick的选择（如23:00 vs 22:59:59.500）
  - on_tick可能更准确：使用时间顺序上真正的最后一个tick

  3. Volume和Turnover累计规则

  - ✅ 新交易日开始时重置（夜盘集合竞价）
  - ✅ 同一交易日内连续累计（日盘集合竞价）
  - ✅ Volume和Turnover同步重置
