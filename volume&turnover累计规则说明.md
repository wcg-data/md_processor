# 中国期货Volume和Turnover累计规则说明

## 核心规则

### ✅ 正确的规则（已验证）

**中国期货的交易日定义**：
- **交易日N** = N日晚上21:00夜盘 + N+1日白天日盘
- 例如："2024-03-05交易日" 包括：
  - 2024-03-05 晚上21:00-23:00（或次日凌晨）夜盘
  - 2024-03-06 白天09:00-15:00日盘

**Volume和Turnover累计规则**：
1. **每个交易日开始时volume和turnover从0（或小值）开始累计**
2. **夜盘→日盘：volume和turnover连续累计**（同一交易日内）
3. **日盘结束→下一夜盘：volume和turnover同时重置**（新交易日开始）

**重要**：Volume和Turnover遵循**完全相同**的累计规则！

## 实际验证数据（rb2412）

### 正常交易日示例：2024-03-05交易日

```
时间                          Volume    Turnover        说明
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2024-03-05 20:59  (夜盘开始)   1500     55710000       ← 本交易日起点
2024-03-05 22:59  (夜盘结束)   1549     57524540       ← 夜盘累计（+49手, +1814540元）
2024-03-06 09:00  (日盘开始)   1549     57524540       ✅ 继续累计（同一交易日）
2024-03-06 15:00  (日盘结束)   1xxx     xxxxxxxx       ← 本交易日结束
2024-03-06 20:59  (夜盘开始)   0        0              ✅ 同时重置（新交易日开始）
```

**验证结论**：Volume和Turnover在同一时刻同步累计、同步重置！

### 节假日示例：2024-05-06

```
时间                          Volume    Turnover        说明
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2024-04-30 15:00  (节前最后)   5679     xxxxxxxx       节前最后交易日
[节假日期间无交易]
2024-05-06 08:59  (日盘集合竞价) 300     11031000       ← 新交易日日盘部分
2024-05-06 09:00  (日盘开始)   300      11031000
2024-05-06 15:00  (日盘结束)   xxx      xxxxxxxx
2024-05-06 20:59  (夜盘集合竞价) 800     29968000       ← 下一交易日夜盘部分
2024-05-06 21:00  (夜盘开始)   800      29968000
```

**说明**：
- 2024-05-06这个日期包含了两个交易日的数据：
  - 早上：2024-05-06交易日的日盘（无前面的夜盘，因为节前停夜盘）
  - 晚上：2024-05-07交易日的夜盘
- Volume和Turnover在节假日场景下也保持同步重置

## 对代码逻辑的影响

### ✅ 当前代码逻辑是正确的

```rust
// bar1min_aggregator.rs line 246-256
// Volume处理
let vol = if last_tick.volume < prev.volume {
    last_tick.volume  // volume重置，直接使用
} else {
    last_tick.volume.saturating_sub(prev.volume)  // 正常差分
};

// Turnover处理（完全相同的逻辑）
let to = if last_tick.turnover < prev.turnover {
    last_tick.turnover  // turnover重置，直接使用
} else {
    last_tick.turnover - prev.turnover  // 正常差分
};
```

**为什么这个逻辑正确**：

1. **正常情况**（夜盘→日盘，同一交易日内）：
   - `current >= prev`（volume和turnover都是）
   - 执行差分：`current - prev`
   - ✅ 正确

2. **交易日切换**（日盘结束→下一夜盘）：
   - `current < prev`（例如：volume从1549重置到0，turnover从57524540重置到0）
   - 直接使用`current`值
   - ✅ 正确

3. **节假日后恢复**：
   - 如果日盘开始时 value < 前一交易日结束value
   - 直接使用当前value
   - ✅ 正确

**重要**：Volume和Turnover使用相同的逻辑处理，因为它们遵循相同的累计规则！

### 通用性验证

**所有交易所、所有品种都遵循这个规则**：
- ✅ SHFE（上期所）：cu、rb、ag等
- ✅ DCE（大商所）：m、y等
- ✅ CZCE（郑商所）：CF、TA等
- ✅ CFFEX（中金所）：IF、IC、IH等

**无夜盘品种**：
- 只有日盘交易
- 每天日盘开始时volume从0开始
- 规则一致：新交易日volume重置

## 关键结论

### ❌ 之前的错误理解
"夜盘和日盘分别累计，夜盘→日盘会重置"

### ✅ 正确的理解
"每个交易日volume和turnover连续累计，交易日之间同步重置"

### ✅ 代码设计的优势

我们的代码使用的是**数据驱动**的设计：
```rust
// Volume和Turnover都使用相同的逻辑
if last_tick.volume < prev.volume {
    // 检测到减少，说明发生了重置
    last_tick.volume
} else {
    // 正常差分
    last_tick.volume - prev.volume
}
```

这个设计的优势：
1. **不需要判断交易日边界**
2. **不需要判断是否节假日**
3. **不需要判断是否停夜盘**
4. **自动适配所有情况**
5. **Volume和Turnover使用相同逻辑**

只要检测到`value减少`，就知道发生了重置，直接使用当前值即可。

## 最终验证

✅ **Volume和Turnover遵循完全相同的累计规则**
✅ **逻辑完全正确**，适用于：
- 所有交易所
- 所有品种（有夜盘/无夜盘）
- 正常交易日
- 节假日前后
- 停夜盘/恢复夜盘

## 实际测试覆盖

已验证的合约：
- **rb2412**（SHFE，有夜盘）：正常交易日 + 节假日场景
- **cu1708**（SHFE，有夜盘）：跨月验证
- **y1701**（DCE，有夜盘）：不同交易所验证

**结论**：所有合约Volume和Turnover表现一致，规则通用！
